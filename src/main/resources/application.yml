spring:
  application:
    name: chat-service
  datasource:
    url: jdbc:mysql://localhost:3306/chat_db
    username: root
    password: ehskfem1!
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 80
      minimum-idle: 30
      idle-timeout: 300000
      max-lifetime: 900000
      connection-timeout: 10000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true

  data:
    mongodb:
      uri: mongodb://root:secret123@localhost:27017/park_mate_chat_db?authSource=admin
      auto-index-creation: true

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
